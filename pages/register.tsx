import Head from "next/head";
import React from "react";
import { Layout } from "../src/components/layout";

const register = () => {
	return (
		<>
			<Head>
				<title>reg</title>
			</Head>
			<Layout>registe</Layout>
		</>
	);
};

export default register;
